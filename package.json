{
  "name": "aria-music",
  "version": "0.1.0",
  "description": "Apple Music CLI for power users and AI agents",
  "type": "module",
  "bin": {
    "aria": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile aria",
    "link": "bun link"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
